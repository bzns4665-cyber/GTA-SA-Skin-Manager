name: Android CI Build

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'
        cache: 'gradle'
    
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
      
    - name: Make gradlew executable
      run: |
        chmod +x gradlew || echo "Gradlew not found"
        ls -la
    
    - name: Initialize Gradle Wrapper
      run: |
        gradle wrapper --gradle-version 8.2
        chmod +x gradlew
    
    - name: Build Debug APK
      run: |
        ./gradlew assembleDebug --stacktrace --info
    
    - name: Verify APK exists
      run: |
        echo "Searching for APK files..."
        find . -name "*.apk" -type f
        ls -lah app/build/outputs/apk/debug/ || echo "Debug folder not found"
    
    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: app-debug-apk
        path: app/build/outputs/apk/debug/app-debug.apk
        retention-days: 30
    
    - name: Create Release
      if: success()
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: build-${{ github.run_number }}
        release_name: "GTA SA Skin Manager v1.0.${{ github.run_number }}"
        body: |
          # 🎮 GTA SA Skin Manager - إصدار تلقائي
          
          ## 📥 التحميل
          حمّل ملف **app-debug.apk** من الأسفل وثبته على هاتفك الأندرويد.
          
          ## ✨ المميزات
          - ✅ استبدال سكنات الشخصيات في GTA San Andreas
          - ✅ دعم ملفات DFF و TXD
          - ✅ واجهة عربية سهلة
          
          ## 📱 التثبيت
          1. حمّل app-debug.apk
          2. فعّل "مصادر غير معروفة" في الإعدادات
          3. ثبّت التطبيق
          4. امنح صلاحيات الوصول للملفات
          
          **Build:** #${{ github.run_number }}
        draft: false
        prerelease: false
    
    - name: Upload Release APK
      if: success()
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./app/build/outputs/apk/debug/app-debug.apk
        asset_name: app-debug.apk
        asset_content_type: application/vnd.android.package-archive
