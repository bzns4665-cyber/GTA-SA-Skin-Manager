name: Android CI Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        cache: 'gradle'
    
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew || echo "No gradlew found, will use gradle wrapper action"
    
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3
      with:
        gradle-version: wrapper
    
    - name: Build with Gradle
      run: ./gradlew assembleDebug --stacktrace --no-daemon
      
    - name: List APK files
      run: |
        echo "Searching for APK files:"
        find . -name "*.apk" -type f
    
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: GTA-SA-Skin-Manager-APK
        path: app/build/outputs/apk/debug/*.apk
        retention-days: 30
    
    - name: Create GitHub Release
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v1.0.${{ github.run_number }}
        name: GTA SA Skin Manager v1.0.${{ github.run_number }}
        body: |
          # 🎮 GTA SA Skin Manager - Build #${{ github.run_number }}
          
          ## 📥 تحميل التطبيق
          حمّل ملف **app-debug.apk** من الأسفل وثبته على هاتفك الأندرويد.
          
          ## ✨ المميزات
          - ✅ استبدال سكنات الشخصيات في GTA San Andreas للأندرويد
          - ✅ دعم ملفات DFF (النماذج) و TXD (الخامات)
          - ✅ واجهة سهلة الاستخدام
          - ✅ يعمل مع اللعبة المثبتة من Google Play
          
          ## 📱 طريقة التثبيت
          1. حمّل ملف **app-debug.apk**
          2. افتح الإعدادات → الأمان → فعّل "السماح بالتثبيت من مصادر غير معروفة"
          3. افتح الملف المحمّل وثبّت التطبيق
          4. عند فتح التطبيق، امنحه صلاحيات الوصول للملفات
          
          ## 🔧 Build Info
          - **Build Number:** #${{ github.run_number }}
          - **Commit:** ${{ github.sha }}
          - **Date:** ${{ github.event.head_commit.timestamp }}
        files: app/build/outputs/apk/debug/*.apk
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}