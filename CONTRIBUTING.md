# المساهمة في GTA SA Skin Manager

شكرًا لاهتمامك بالمساهمة في هذا المشروع! 🚀

## كيفية المساهمة

### 1. Fork المستودع

```bash
# اضغط على زر "Fork" في الأعلى ، ثم استنسخ المستودع الخاص بك
git clone https://github.com/YOUR_USERNAME/GTA-SA-Skin-Manager.git
cd GTA-SA-Skin-Manager
```

### 2. إنشاء Branch جديد

```bash
# استخدم اسم وصفي للـ branch
git checkout -b feature/your-feature-name
# أو
git checkout -b fix/bug-description
```

### 3. إجراء التغييرات

- اكتب كود نظيف ومقروء
- اتبع نمط الكود الحالي
- أضف تعليقات واضحة
- اختبر تغييراتك جيدًا

### 4. Commit التغييرات

```bash
git add .
git commit -m "وصف واضح للتغييرات"
```

**نمط رسائل Commit:**

- `feat:` - ميزة جديدة
- `fix:` - إصلاح bug
- `docs:` - تحديث التوثيق
- `style:` - تغييرات في التنسيق
- `refactor:` - إعادة هيكلة الكود
- `test:` - إضافة اختبارات
- `chore:` - مهام صيانة

**أمثلة:**
```
feat: Add vehicle skin support
fix: Fix IMG archive reading on Android 11+
docs: Update README with new features
```

### 5. Push التغييرات

```bash
git push origin feature/your-feature-name
```

### 6. فتح Pull Request

- اذهب إلى مستودعك على GitHub
- اضغط "New Pull Request"
- املأ النموذج بوصف واضح

## معايير الكود

### Java Code Style

- استخدم camelCase للمتغيرات والدوال
- استخدم PascalCase للفئات (Classes)
- استخدم UPPER_SNAKE_CASE للثوابت
- مسافة بادئة 4 فراغات

```java
// جيد
public class IMGArchive {
    private static final int SECTOR_SIZE = 2048;
    private List<IMGEntry> entries;
    
    public boolean replaceFile(String fileName, byte[] data) {
        // ...
    }
}

// سيئ
public class imgarchive {
    private int sectorsize = 2048;
}
```

### XML Style

- استخدم snake_case لل IDs
- استخدم بادئات واضحة (btn_, tv_, edit_, etc.)
- رتب الخصائص بشكل منطقي

```xml
<!-- جيد -->
<Button
    android:id="@+id/btn_install"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:text="@string/install" />
```

## مجالات المساهمة

### مميزات مقترحة:

1. **دعم المركبات**
   - إضافة إمكانية تغيير سكنات السيارات
   - معالجة ملفات handling.cfg

2. **معاينة النماذج**
   - عرض 3D للنموذج قبل التثبيت
   - معاينة الخامات

3. **إدارة النسخ الاحتياطية**
   - نسخ احتياطي تلقائي قبل الاستبدال
   - استعادة سريعة

4. **مكتبة سكنات**
   - تخزين وتنظيم السكنات
   - معاينة ومقارنة

5. **التحميل من الإنترنت**
   - تكامل مع GTAInside
   - تحميل مباشر للسكنات

6. **تحسينات الأداء**
   - تحسين سرعة معالجة IMG
   - تقليل استهلاك الذاكرة

### إصلاح الأخطاء:

- مشاكل الصلاحيات على Android 11+
- دعم مسارات مختلفة للعبة
- معالجة ملفات معطوبة
- تحسين رسائل الخطأ

### التوثيق:

- تحديث README.md
- إضافة أمثلة للكود
- ترجمة للغات أخرى
- شروح فيديو

## الاختبار

قبل إرسال Pull Request:

1. تأكد من بناء التطبيق بنجاح
2. اختبر على أجهزة مختلفة إن أمكن
3. تأكد من عمل الميزة كما متوقع
4. تحقق من عدم وجود تعارضات مع الميزات الحالية

### بيئة الاختبار:

- Android 11 (API 30)
- Android 12 (API 31)
- Android 13 (API 33)
- Android 14 (API 34)

## إرشادات عامة

### ما يجب فعله:

✅ اكتب كود نظيف ومقروء  
✅ أضف تعليقات للأجزاء المعقدة  
✅ اتبع نمط الكود الحالي  
✅ اختبر تغييراتك  
✅ حدّث التوثيق  

### ما يجب تجنبه:

❌ تغييرات كبيرة بدون مناقشة  
❌ كود غير موثّق  
❌ إزالة ميزات موجودة  
❌ تغيير بنية المشروع بدون سبب  

## الاتصال

إذا كان لديك أسئلة أو اقتراحات:

- افتح Issue على GitHub
- انضم إلى نقاشات Discussions

---

**شكرًا لمساهمتك في تحسين التطبيق!** 🚀✨